const more = (n) => n + 1;
const resultMore = more(4);

const less = (n) => n - 1;
const resultLess = less(4);

const add = (m,n) => m + n;
const resultAdd = add(4,2);

const sub = (m,n) => m - n;
const resultSub = sub(4,2)


